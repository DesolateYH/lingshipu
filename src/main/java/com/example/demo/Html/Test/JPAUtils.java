package com.example.demo.Html.Test;/*
package com.example.demo.Html;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

public class JPAUtils {

    private static EntityManagerFactory entityManager;

    static {
        //读取主配置,并创建EntityManagerFactory(可以理解为Session工厂)
        try{
            entityManager = Persistence.createEntityManagerFactory("com.example.demo.Html.Entity");
        }catch (Exception e){
            e.printStackTrace();
            throw new RuntimeException("解析配置文件或者持久对象的配置出现异常：" + e.getMessage());
        }
    }

    public static EntityManager getEntityManager() {
        return entityManager.createEntityManager();
    }
}
	
*/
